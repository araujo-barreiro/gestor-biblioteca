void base(char,int ); //funcion base de pantalla
void lado(char,int ); //funcion lado de pantalla
void titulo(char *,char,int); //funcion lado + texto de pantalla
void pantalla(); // dibuja un recuadro
int confirmar(); //funcion para controlar la salida del programa
